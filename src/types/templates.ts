export interface ImageTemplate {
  key: string;
  label: string;
  id: string;
  description: string;
}

export interface NetworkTemplate {
  key: string;
  label: string;
  id: string;
  description: string;
}
